(trec thing a : int =>
typecase [d.int] a of
| int => 1
| bool => 2
| str => 3
| a -> b => (thing [a]) + (thing [b])
| a * b => (thing [a]) * (thing [b])
| list a => (thing [a]) * 100
end)
[str * (bool -> bool)]
